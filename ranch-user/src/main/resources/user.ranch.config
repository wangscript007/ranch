## 设置绑定ID有效时长，单位：秒。
## 当前时间距最近绑定时间超过设置值时，绑定将实效。
## 默认有效时长为一周。
#ranch.user.auth.bind.effective = 604800

## 设置在线时间有效时长，单位：分钟。
## 最后一次访问超过此设置时，将被视为下线。
#ranch.user.online.effective = 30

## 设置微信服务key。
#ranch.weixin.key = ranch.weixin
